<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Procurar Treino</title>
  <style>
    /* CSS Geral */
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
      min-height: 100vh;
    }

    /* Container principal */
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
      max-width: 1200px; /* Limita a largura em telas grandes */
      padding: 20px;
      box-sizing: border-box;
    }

    h2 {
      color: #a044e8;
      font-size: 2em;
      margin-top: 20px;
      text-align: center;
    }

    /* Estilo da barra de categorias */
    .categories {
      display: flex;
      gap: 15px;
      flex-wrap: wrap; /* Permite quebrar a linha se necessário */
      justify-content: center;
      margin: 20px 0;
      width: 100%;
    }

    .category, .filter {
      padding: 10px 20px;
      border: 2px solid #a044e8;
      border-radius: 20px;
      color: #a044e8;
      cursor: pointer;
      font-weight: bold;
      text-align: center;
    }

    /* Estilo para tela menor */
    @media (max-width: 600px) {
      .category, .filter {
        padding: 8px 16px;
        font-size: 0.9em;
      }

      h2 {
        font-size: 1.8em;
      }
    }
  </style>
</head>
<body>

<div class="container">
  <h2>Procurar um treino</h2>

  <!-- Filtros de categorias -->
  <div class="categories">
    <div class="category selected">Academia</div>
    <div class="category">Calistenia</div>
    <div class="category">Alongamentos</div>
    <div class="category">Misto</div>
    <div class="filter">Filtros &#x1F50D;</div>
  </div>
</div>

</body>
</html>
